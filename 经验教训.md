# 项目经验教训

> 本文件沉淀本项目踩过的坑与解决思路，供后续开发（尤其是本仓库任何会话）直接调用。
> 每条经验包含：现象 → 根因 → 修复 → 可复用的检查清单。

---

## 经验 1：PWA Service Worker「缓存优先」导致 dev / build 表现不一致（最坑）

### 现象
- 同一个 UI 在 `pnpm dev`（http://localhost:5173）下显示**旧内容**（旧图片、旧样式、旧逻辑）；
- 双击 `dist/index.html`（file://）打开**永远正常**；
- 我（开发者）只验证了构建版，用户只看 dev 版 → 双方看到的东西完全不同，互相"对不上"。

### 根因（三层叠加）
1. **SW 注册条件太宽**：`location.protocol.startsWith('http')` 在 dev 模式下也为 true，
   于是 **Vite dev server 也会注册 Service Worker**（`public/sw.js` 会被 dev server 当作静态资源从根路径提供）。
2. **SW 的 fetch 策略是 cache-first**：`caches.match(request)` 命中即返回缓存，**永远不重新校验**。
   图片/CSS/模块 URL 一旦被缓存，之后磁盘上文件怎么改都不会生效。
3. **资源 URL 不变**：Vite 对 `src/assets/*.png` 的 import 在 dev 下返回固定路径 `/src/assets/qianxia.png`，
   每次重新生成图片（内容变了但文件名没变）→ 浏览器永远拿到 SW 缓存里的第一版。
   构建版则把图片内联成 base64 data URI，`data:` 请求 SW 缓存不到、也不参与网络缓存 → 永远最新。

### 修复
- `src/view/main.ts`：**只在 `import.meta.env.PROD` 时注册 SW**；dev 下改为
  `navigator.serviceWorker.getRegistrations().then(rs => rs.forEach(r => r.unregister()))`，
  主动注销可能残留的旧 SW，避免开发环境被缓存污染。
- `public/sw.js`：fetch 策略从 **cache-first 改为 network-first + 离线回退缓存**：
  - 在线时永远走网络（配合 ETag 协商，天然最新）；
  - 断网时回退到缓存（离线可用性保留）；
  - 只缓存核心资源（页面/清单/图标），且只拦截**同源**请求。
- 缓存桶升级 `bhs-clan-v1 → v2`，activate 时清理旧桶。

### 关于"旧 SW 已经注册了怎么办"
旧 SW 会用缓存先行的方式把旧页面吐出来，新代码（含注销逻辑）根本跑不到。
- 浏览器每次导航都会做 SW 更新检查（绕过 SW 自身），只要 `sw.js` 内容变了，新 SW 就会安装并
  `skipWaiting` 激活、清掉旧缓存 → **刷新两次即可自愈**；
- 仍然卡死时手动清理：DevTools → Application → Service Workers → Unregister（一次性操作）。

### 可复用检查清单
- [ ] SW 的 fetch 策略默认用 **network-first**（或 stale-while-revalidate），不要裸 cache-first；
- [ ] 开发环境（`import.meta.env.DEV`）**绝不注册 SW**，且注销残留注册；
- [ ] SW 更新时**升级缓存桶名**并在 activate 里清理旧桶；
- [ ] 验证 "dev 和 build 表现一致" 时要同时跑两种模式，且**用全新浏览器上下文**（清缓存/无痕）验证；
- [ ] 图片等二进制资源若会被重新生成，URL 必须内容相关（hash）或走网络优先。

---

## 经验 2：视觉问题在没有视觉模型时如何"看见"截图

### 现象
- 本会话模型不支持读图，modlens / describe-image 也未配置，用户贴图后无法直接判断。

### 解法（程序化"看"图）
- **粗网格 ASCII 还原**：把截图按亮度/色相分类（`#` 深色背景、`R` 粉红皮肤、`.` 高亮、`o` 中间色），
  以 40~60 列的字符画输出，即可辨认布局（哪是立绘条、哪是文字行、角色是否完整、脸在哪）。
- **皮肤色区域扫描**：统计 R>170、G/B 居中、R-G 差>25 的像素包围盒，定位"脸+身体"区域。
- **像素级对比**：dev 与 build 两版截图逐点对比（RGB 差>30 计数），判断渲染是否一致。
- **实测渲染**：用 Playwright（系统 Edge）真实打开游戏截图，拿到"真相"，而不是猜。

### 教训
遇到"用户说图有问题"而自己又看不到图时：先程序化分析 + 实测渲染，再动手改，不要瞎猜参数。

---

## 经验 3：Windows 下 PowerShell 的坑

### 现象与规避
- **PS 5.1 变量名不区分大小写**：脚本里同时用 `$r`（行计数）和 `$R`（红色通道）会互相覆盖，
  导致循环失控/输出全部相同。→ 变量名不要只靠大小写区分（`$row`/`$rC`）。
- **`??` 空合并运算符 PS 5.1 不支持**（PS7 才支持）。
- **中文在 .ps1 里可能被 ANSI 编码搞乱** → 脚本用纯 ASCII，路径/文本用参数传入。
- **Add-Type 编译 C# 需要 `-ReferencedAssemblies`（System.Drawing 路径）**，且默认不开 unsafe。
- **System.Drawing 用 GetPixel 逐点扫描很慢** → 隔行采样 / LockBits + Marshal.Copy。

---

## 经验 4：开发辅助脚本注意 Vite 文件监听

- 用 write 工具直接改 `public/` 下的文件时，会先写临时文件（同目录 `*.tmp`），
  Vite 的 watch 可能对该临时文件报 `EBUSY` 并**整个 dev server 崩溃**。
- 规避：改 `public/` 内文件时若 dev server 在跑，改完重启 dev server；或先停 dev 再改。
