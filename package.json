{
  "name": "mint-clan-convention",
  "private": true,
  "version": "0.1.0",
  "description": "《薄荷色氏族公约》—— 商业级肉鸽卡牌爬塔（纯静态网页版）",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "pnpm test && tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "validate": "vitest run tests/content-validation.test.ts",
    "e2e": "node e2e/smoke.mjs"
  },
  "devDependencies": {
    "jsdom": "^30.0.1",
    "playwright": "^1.62.1",
    "typescript": "^5.6.0",
    "vite": "^6.0.0",
    "vite-plugin-singlefile": "^2.3.3",
    "vitest": "^2.1.0"
  }
}
